# Backend Environment Variables Example
# Copy this file to .env and adjust values

# Server
PORT=4000
NODE_ENV=production
# CORS_ORIGIN accepts:
# - Comma separated exact origins
# - '*' to allow all (not recommended for production)
# - Wildcard subdomains using *.domain.tld (e.g. *.vercel.app)
CORS_ORIGIN=https://find-my-spot-tau.vercel.app,https://find-my-spot-lac.vercel.app,http://localhost:5173,*.vercel.app
JWT_SECRET=replace-with-a-long-random-string

# Database (MySQL on Railway) - production
DB_DIALECT=mysql
DB_HOST=your-railway-mysql-host
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=railway
# Railway MySQL URL (if available, overrides the above)
MYSQL_URL=
DATABASE_URL=
DB_SSL=true
DB_POOL_MAX=15

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120

# Development (SQLite) example - uncomment for local dev without MySQL
# NODE_ENV=development
# DB_DIALECT=sqlite
# DB_STORAGE=database.sqlite
# (When using SQLite, leave MYSQL_URL / DATABASE_URL blank)
