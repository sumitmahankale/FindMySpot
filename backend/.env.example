# Backend Environment Variables Example
# Copy this file to .env and adjust values

# Server
PORT=10000
NODE_ENV=production
CORS_ORIGIN=https://find-my-spot-lac.vercel.app
JWT_SECRET=replace-with-a-long-random-string

# Database (MySQL on Railway)
DB_DIALECT=mysql
DB_HOST=your-railway-mysql-host
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=railway
# Railway MySQL URL (if available, overrides the above)
MYSQL_URL=mysql://root:password@host:port/railway
DATABASE_URL=mysql://root:password@host:port/railway
DB_SSL=true
DB_POOL_MAX=15

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120

# OR use SQLite locally for development (comment MySQL vars above if using SQLite)
# DB_DIALECT=sqlite
# DB_STORAGE=database.sqlite
